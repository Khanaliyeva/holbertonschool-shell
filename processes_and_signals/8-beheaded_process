#!/usr/bin/env bash
#8

# Find PIDs of 7-highlander processes
pids=$(ps aux | grep '[7]-highlander' | awk '{print $2}')

# If PIDs found, kill them
if [ -n "$pids" ]; then
  for pid in $pids; do
    kill "$pid" 2>/dev/null
  done
fi
